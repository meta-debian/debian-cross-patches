diff --git a/unifdef-2.10/debian/rules b/unifdef-2.10/debian/rules
index 17f632a..1e4e7d0 100755
--- a/unifdef-2.10/debian/rules
+++ b/unifdef-2.10/debian/rules
@@ -1,5 +1,9 @@
 #!/usr/bin/make -f
 
+ifneq ($(DEB_BUILD_GNU_TYPE),$(DEB_HOST_GNU_TYPE))
+        export  CC = $(DEB_HOST_GNU_TYPE)-gcc
+endif
+
 build: build-arch build-indep
 
 build-indep: build-stamp
@@ -8,7 +12,11 @@ build-arch: build-stamp
 
 build-stamp:
 	$(MAKE) $(shell dpkg-buildflags --export=cmdline)
+
+ifeq ($(DEB_HOST_GNU_TYPE),$(DEB_BUILD_GNU_TYPE))
 	LC_ALL=C $(MAKE) test
+endif
+
 	touch build-stamp
 
 clean:
