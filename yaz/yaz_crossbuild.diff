diff --git a/yaz-4.2.30/debian/rules b/yaz-4.2.30/debian/rules
index 3b9d48b..f9423c8 100755
--- a/yaz-4.2.30/debian/rules
+++ b/yaz-4.2.30/debian/rules
@@ -10,6 +10,10 @@ DH_OPTIONS=--with quilt --with autotools_dev
 
 export CFLAGS+= -Wall
 
+ifneq ($(DEB_BUILD_GNU_TYPE),$(DEB_HOST_GNU_TYPE))
+	PKG_CONFIG_PATH = /usr/lib/$(DEB_HOST_GNU_TYPE)/pkgconfig
+endif
+
 %:
 	dh $@ $(DH_OPTIONS)
 
